Node.js server consuming https://restcountries.com/ api - return all countries that match

## Install dependencies
run "npm install" in root directory

## Open app
app is running at http://localhost:3000/
